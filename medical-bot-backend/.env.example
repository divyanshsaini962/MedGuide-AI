# Backend example env for medical-bot-backend
# Copy this file to .env (at project root) and replace placeholders with real values.

# Port the server should listen on
PORT=8080

# JWT secret used to sign tokens (required)
JWT_SECRET=change_this_to_a_secure_secret

# Google client ID to verify tokens received from frontend (required)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: if you implement server-side OAuth code exchange, set the client secret here
# GOOGLE_CLIENT_SECRET=your-client-secret
